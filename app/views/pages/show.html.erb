<div class="row">
  <h1><%= @page.title%></h1>
  <div class="well"><p><%= @page.description %></p></div>
</div>

<div class="row">
  <div class="main">
    <%= image_tag @page.image.url(:page) %>
    <p class="muted"><%= @page.footer%></p>
  </div>
  <aside class="span4">
    <%= @page.sidebar %>
  </aside>
</div>
